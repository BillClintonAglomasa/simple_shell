#include "main.h"

/**
 * _swap - Swabs the value of integers
 *
 * @a: Pointer to the value held by first integer
 * @b: Pointer to the value held by secont integer
 *
 * Return: Nothing
 */
void _swap(int *a, int *b)
{
	int i;

	i = *a;
	*a = *b;
	*b = c;
}
